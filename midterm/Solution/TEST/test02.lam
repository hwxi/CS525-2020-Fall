fun
main() =
let
val
fibo =
fix
f(x:int): int =>
if x >= 2 then f(x-1)+f(x-2) else x
in
  let
  val
  fibo10 = fibo(10)
  in
    let
    val _ =
    print("fibo10 = ")
    in
    let val _ = print(fibo10) in print("\n") end
    end
  end
end // end of [let]
